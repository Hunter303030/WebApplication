@model ProfileEditDto
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Редактировать</title>
    <link rel="stylesheet" href="~/css/Profile/Edit.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.16/jquery.mask.min.js"></script>
</head>
<body>
    <div class="form-edit">
        <div class="cont">
            <div class="form-data">
                <form method="post" asp-controller="Profile" enctype="multipart/form-data">
                    <label class="title">Редактировать</label>
                    <div class="input-box">
                        <img class="input-icon" src="~/Image/icon-avatar.png" alt="Alternate Text" />
                        <input asp-for=@Model.NickName class="input-data" type="text" required />
                        <span class="input-title">Псевдоним</span>
                    </div>
                    <div class="input-box">
                        <img class="input-icon" src="~/Image/icon-email.png" alt="Alternate Text" />
                        <input asp-for=@Model.Email class="input-data" type="email" required />
                        <span class="input-title">Почта</span>
                    </div>
                    <div class="input-box">
                        <img class="input-icon" src="~/Image/icon-telephone.png" alt="Alternate Text" />
                        <input asp-for="@Model.Phone" id="phone-input" class="input-data" type="text" required placeholder="+7 (-_) -_----" />
                        <span class="input-title">Телефон</span>
                    </div>                                  
                    <img id="current-avatar" class="preview" src="@Url.Content(@Model.ImageUrl)" alt="Аватарка по умолчанию" />
                    <div class="input-box">
                        <label for="fileInput" class="custom-file-upload">
                            Выберите файл
                        </label>    
                        <input asp-for=@Model.ImageUrl type="hidden"/>
                        <input asp-for=Avatar type="file" id="fileInput" class="file-input" accept="image/*">
                        <label id="file-name">Файл не выбран</label>
                    </div>
                    <div class="input-box">
                        <button type="button" id="resetAvatar">Вернуть исходную аватарку</button>
                    </div>
                    <a class="change-pasword" href="@Url.Action("EditPasswordView","Profile")">Изменить пароль</a>
                    <input asp-action="Edit" class="btn-edit" type="submit" value="Сохранить" />
                </form>
            </div>
        </div>
    </div>
    <script src="~/js/Profile/Edit.js"></script>
</body>
</html>